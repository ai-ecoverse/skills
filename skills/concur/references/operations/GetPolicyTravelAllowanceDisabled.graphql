query GetPolicyTravelAllowanceDisabled($policyId: ID!, $userId: String!, $contextRole: ContextRoleType!) {
  expensePolicy(policyId: $policyId, userId: $userId, contextRole: $contextRole) {
    disableTravelAllowance
    id
    __typename
  }
}